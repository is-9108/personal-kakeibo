# Backend
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1

# Frontend
streamlit>=1.28,<2

# Database & ORM
sqlalchemy>=2.0,<3

# Scheduler (fixed costs, monthly reports)
apscheduler>=3.10,<4

# Charts (Streamlit + reports)
plotly>=5.18,<6

# HTTP client (Streamlit → FastAPI)
httpx>=0.27,<1

# zoneinfo on Windows (Asia/Tokyo for APScheduler)
tzdata>=2024.1
